# Тестовое задание для HARZ Labs

## Команды для сборки
Установка зависимостей: `npm i`
Запуск локального сервера: `npm start`
Сборка без запуска локального сервера: `npm run build`

Скомпилированный код попадает в папку `dist/`
